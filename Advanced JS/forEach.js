const names = ['htin', 'win', 'min', 'myat'];

for (let i = 0; i < names.length - 1; i++){
    const name = names[i];
    //do something with each name
}

names.forEach(name => {
    //do something with each name
    console.log(name);
});